{
  // See: https://go.microsoft.com/fwlink/?linkid=830387
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Python: Debug ZMK Build Script",
      "type": "debugpy",
      "request": "launch",
      "program": "${workspaceFolder}/build-firmware.py",
      "cwd": "${workspaceFolder}",
      // critical -- sets cwd to the west working directory, which must be 'zmk')
      "console": "integratedTerminal",
      "justMyCode": true
    }
  ]
}
